# `pull_team_organizations()` returns character vector of organization names

    Code
      orgs_by_team <- gitlab_env$pull_team_organizations(team)
    Message <cliMessage>
      > Pulling organizations by team.
      v Pulled 1 organizations.

