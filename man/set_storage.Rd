% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gitstats_functions.R
\name{set_storage}
\alias{set_storage}
\title{Set storage backend}
\usage{
set_storage(gitstats, type = "local", ...)
}
\arguments{
\item{gitstats}{A GitStats object.}

\item{type}{A character: \code{"local"} (default, in-memory), \code{"postgres"}
(PostgreSQL database), or \code{"sqlite"} (SQLite file or in-memory database).}

\item{...}{For \code{"postgres"}: connection arguments passed to
\code{DBI::dbConnect(RPostgres::Postgres(), ...)} (e.g. \code{dbname}, \code{host},
\code{port}, \code{user}, \code{password}) and optionally \code{schema} (character, defaults
to \code{"git_stats"}). For \code{"sqlite"}: \code{dbname} (path to SQLite file,
defaults to \code{":memory:"}).}
}
\value{
A \code{GitStats} object.
}
\description{
Set the storage backend for a \code{GitStats} object. By default,
data is stored in memory. Use \code{type = "postgres"} to persist data in a
PostgreSQL database.
}
\examples{
\dontrun{
  my_gitstats <- create_gitstats() |>
    set_storage(
      type = "postgres",
      dbname = "my_database",
      host = "localhost",
      user = "postgres",
      password = "secret"
    )
}
}
