# Show hosts set in `GitStats`

Retrieves hosts set by `GitStats` with `set_*_host()` functions.

## Usage

``` r
show_hosts(gitstats)
```

## Arguments

- gitstats:

  A GitStats object.

## Value

A list of hosts.
