# Show organizations set in `GitStats`

Retrieves organizations set or pulled by `GitStats`. Especially helpful
when user is scanning whole git platform and wants to have a glimpse at
organizations.

## Usage

``` r
show_orgs(gitstats)
```

## Arguments

- gitstats:

  A GitStats object.

## Value

A vector of organizations.
